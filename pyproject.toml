[project]
name = "detroit"
description = "detroit is wrapper for Python of d3js especially focus on Observable Plot in the current version."
version = "1.0.0"
readme = "README.md"
authors = [
  { name = "Benjamin BOURBON", email = "ben.bourbon06@gmail.com"},
]
requires-python = ">=3.9"
dependencies = [
  "jinja2",
  "markupsafe",
  "playwright",
  "quart",
]
license = {file = "LICENSE"}

[tool.setuptools.packages.find]
include = ["detroit*"]
